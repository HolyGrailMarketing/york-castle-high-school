{
  "buildCommand": "npm run build",
  "installCommand": "npm install && cd admin-dashboard && npm install && cd ../backend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
